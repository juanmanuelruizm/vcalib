name: p2_stc_P12_g5_lv3
dataset: data/datasets/level_1_vs_level_3
input_size: 384
filter:
  type: spatial_tone_curve
  P: 12
  grid_size: 5
layer_group:
  name: phase2_group
  layers:
  - backbone.projector
training:
  max_epochs: 50
  learning_rate: 0.005
  reg_weight: 0.01
  early_stopping_patience: 10
  seed: 42
  checkpoint_every: 10
output:
  results_dir: results/experiments
